Framework :- Which provides the flow of software application and tells the developer what it needs and calls the code which is provided by the developer as required.

Library :- the application calls the code from the library

CDN :- Content delivery network , which provide cached internet content from a network location to user to speedup its delivery.

CROSS ORIGIN - which provides better error handling experience.

class -3
NPM:-is a default package manager for javascript runtime env n0de js.

Parcel and webpacks are bundlers which are used for javascript or typescript code that helps you to minify ,clean, compact the code which makes easier to request to respond to the server is easier


